from setuptools import setup

setup(name='rctobject',
      version='0.1',
      description='Library for the RCT object maker',
      author='Tolsimir',
      license='MIT',
      packages=['rctobject'],
      zip_safe=False)